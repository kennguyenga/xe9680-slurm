# GPU device mapping. NVML autodetect requires the NVIDIA driver installed.
AutoDetect=nvml

# Explicit fallback if AutoDetect is unavailable:
# Name=gpu Type=h100 File=/dev/nvidia0
# Name=gpu Type=h100 File=/dev/nvidia1
# ... through /dev/nvidia7
